# 브라우저(src/index.html CONFIG)에 넣어도 되는 공개 값. 실제 값은 여기에 커밋하지 않는다.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Edge Function 설정값 (supabase secrets set 으로 등록). OPENROUTER_API_KEY 등 비밀 키는 절대 이 파일이나 HTML에 넣지 않는다.
OPENROUTER_MODEL=openrouter/free
DEMO_MODE=true
