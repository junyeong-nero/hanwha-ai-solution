# MoonLight Hanwha 디자인 가이드

MoonLight Hanwha는 한화 그룹사 구성원이 관심사로 모이고, 익명 대화에서 실제 만남으로 이어지는 사내 네트워킹 모바일 웹앱이다. **깊은 남색의 밤하늘, 부드러운 달빛, 한화 주황색으로 연결이 생기는 경험**을 표현한다.

이 문서는 현재 [스타일시트](src/styles.css), [화면 마크업](src/index.html), 화면별 렌더링 코드를 바탕으로 디자인을 정리한다. 기능과 사용자 흐름은 [기능 명세](docs/features.md)가 기준이며, 개발 제약은 [AGENTS.md](AGENTS.md)를 따른다. 아래 구현 수치는 현행 CSS 기준이고, 접근성·확장 규칙은 새 화면을 만들거나 기존 화면을 수정할 때 지킬 기준이다.

## 1. 디자인 방향

### 우주는 관계의 상태를 보여 준다

| 시각 요소 | 의미 | 표현 방식 |
| --- | --- | --- |
| 밤하늘·별 | 동료를 발견하는 공간 | 깊은 남색 배경, 은은한 별, 낮은 강도의 빛 번짐 |
| 행성 | 한화 계열사 | 내 계열사와 아는 사람이 생긴 계열사가 궤도에 등장 |
| 궤도·위성 | 계열사와 동료의 연결 | 관계가 늘면 행성과 위성이 추가됨 |
| 행성 점등 | 실제 만남으로 생긴 연결 | 밝기와 광채로 연결 활성화를 표현 |
| 달빛·베일 벗기기 | 익명에서 서로 아는 사이로의 전환 | 만남 완료 시 달빛 연출과 공개 가능한 상대의 실명 표시 |
| 주황 띠 | 모임 안에서 이미 아는 사람의 비율 | 막대 길이와 수치·문구를 함께 표시 |

우주 표현은 홈과 만남 완료 장면에서 가장 풍부하게 사용한다. 모임 탐색·채팅·설정은 제목, 카드, 목록, 버튼의 익숙한 구조로 빠르게 읽고 조작할 수 있게 한다.

### 정보 위계

- 탭마다 하나의 목적을 두고 `상단 제목 → 짧은 설명 → 섹션 → 카드·목록 → 행동` 순서로 읽히게 한다.
- 주황색은 주요 행동, 선택 상태, 아는 얼굴 비율에 집중한다. 모임 목록의 일반 참가 버튼은 중립색으로 유지한다.
- 추천 이유처럼 긴 설명은 상세 시트에 배치한다. 목록에서는 모임 정보와 참가 상태를 우선한다.
- 만남의 성과는 홈의 행성 점등과 연결 수로 다시 보여 준다.

## 2. 색상과 표면

다크 테마를 고정한다. 색상 토큰은 `src/styles.css` 최상단 `:root`에서 관리한다.

### 기본 팔레트

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--bg` | `#090C18` | 가장 깊은 앱 배경, 채팅·의견 화면 |
| `--bg2` | `#0F1326` | 보조 배경 |
| `--card` | `#151A31` | 기본 카드·입력 표면 |
| `--card2` | `#1D2340` | 카드 안의 타일, 보조 버튼, 입력 영역 |
| `--card3` | `#252C4D` | 한 단계 밝은 표면, 아바타·상태 영역 |
| `--line` | `#242B4A` | 기본 테두리 |
| `--line2` | `rgba(255,255,255,.1)` | 강조된 경계 |
| `--hair` | `rgba(255,255,255,.06)` | 목록·카드 내부 구분선 |
| `--tx` | `#F3F5FB` | 제목·주요 본문 |
| `--tx2` | `#A5ACC6` | 설명·메타 정보 |
| `--tx3` | `#6F779A` | 보조 안내·시간·비활성 표현 |

표면은 어두운 배경 위에 조금 더 밝은 카드를 올려 구분한다. 기본 카드는 미세한 상단 광택과 얕은 그림자를 쓰고, 모임 목록 카드는 단색 표면과 테두리로 차분하게 정리한다. 배경의 별은 콘텐츠 뒤에 두며 터치를 받지 않는다.

### 브랜드와 의미 색상

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--orange` | `#F37321` | 브랜드, 선택 테두리, 핵심 신호 |
| `--orange-soft` | `#FF9A57` | 어두운 배경 위 강조 글자·아이콘 |
| `--orange-deep` | `#D9600F` | 짙은 브랜드 보조색 |
| `--orange-a12` / `--orange-a18` / `--orange-a30` | 주황의 12% / 18% / 30% 불투명도 | 약한 강조 배경, 선택 배경, 테두리·포커스 |
| `--moon` / `--moon-a` | `#F5E9C9` / 달빛색 14% | 달, 시스템 안내, 달빛 배지 |
| `--ok` | `#3DDC97` | 완료·전원 가능 |
| `--warn` | `#F5C86B` | 주의·추가 확인 안내 |
| `--err` | `#FF7A7A` | 입력·처리 오류 |
| `--info` | `#7C9CF5` | 정보성 보조색 |

`--grad`는 `135deg` 방향으로 `#FF9A57 0% → #F37321 58% → #E7620F 100%`를 연결한다. 주요 CTA, 내 채팅 말풍선, 진행 막대 등에 사용한다. 달빛의 따뜻한 크림색은 주황색과 역할을 분리해 만남의 전환과 시스템 안내에 사용한다.

색만으로 상태를 전달하지 않는다. 선택은 체크·테두리·문구, 완료는 인원·상태명, 오류는 원인과 다음 행동을 함께 보여 준다.

## 3. 글꼴과 간격

### 타이포그래피

로컬 가변 폰트 **Pretendard**를 사용한다. 폴백은 `-apple-system`, `BlinkMacSystemFont`, `Apple SD Gothic Neo`, `Noto Sans KR`, `sans-serif` 순이다. 기본 본문은 `15px`, 행간 `1.45`, 자간 `-.01em`이다.

| 역할 | 현행 크기·굵기 | 적용 예 |
| --- | --- | --- |
| 탭 제목 | `24px / 800`, 행간 `1.2` | `h1.app` — PC에서는 `27px` |
| 입장 화면 브랜드 | `28px / 800` | MoonLight Hanwha |
| 섹션 제목 | `16px / 800` | `.sec h2`, `.t` |
| 모임 카드 제목 | `16.5px / 800`, 행간 `1.3` | `.meet h3` |
| 주요 버튼 | `15.5px / 800` | `.cta` |
| 설명·카드 메타 | 약 `12.5~13.5px` | `.sub`, `.meta` |
| 보조 안내 | `12px`, 행간 `1.55` | `.hint` |
| 입력 글자 | **최소 `16px`** | 입력·선택·여러 줄 입력 |

제목은 짧고 굵게, 설명은 한 단계 낮은 색으로 둔다. 긴 한글 문장이 들어가는 안내에는 약 `1.5~1.65` 행간을 사용한다. 좁은 화면에서 제목·메타는 줄바꿈을 허용하고, 채팅 목록의 마지막 메시지처럼 한 줄 미리보기가 필요한 영역만 말줄임한다.

### 간격과 형태

- 휴대폰 본문 좌우 여백은 `18px`, 넓은 모바일 화면은 `24px`이다.
- 공용 카드 내부 여백은 `16px`, 카드 사이 간격은 주로 `14px`이다.
- 섹션 제목은 위 `24px`, 아래 `10px`로 그룹을 구분한다.
- 작은 요소 사이에는 주로 `4~8px`, 아이콘·본문 묶음에는 `10~14px` 간격을 쓴다. 별도의 간격 토큰은 현재 없다.

| 토큰 | 값 | 형태·깊이 |
| --- | --- | --- |
| `--r-s` | `10px` | 작은 타일·입력·선택 셀 |
| `--r-m` | `14px` | 모임 카드·중간 크기 묶음 |
| `--r-l` | `20px` | 공용 카드 |
| `--r-xl` | `26px` | 시트 등 큰 표면 |
| `--sh-1` | `0 2px 10px rgba(0,0,0,.25)` | 기본 카드 |
| `--sh-2` | `0 12px 32px rgba(0,0,0,.42)` | 떠 있는 표면·토스트 |
| `--sh-orange` | `0 10px 26px rgba(243,115,33,.32)` | 주요 주황 CTA |

기존 컴포넌트에는 캡슐·원형과 개별 반지름 수치가 남아 있다. 재사용 시 해당 클래스를 사용하고, 새 색상·반지름을 추가해야 하면 먼저 `:root`에 의미 있는 토큰으로 정의한다.

## 4. 프레임과 반응형

기본 검수 화면은 **375×812**다. 앱은 `100dvh` 높이의 프레임 안에서 탭 본문이 스크롤되며, 노치와 홈 인디케이터 영역은 `env(safe-area-inset-*)`로 확보한다.

| 화면 폭 | 앱·본문 | 탐색·콘텐츠 |
| --- | --- | --- |
| `≤430px` | 앱 최대 `520px`, 본문 좌우 `18px` | 하단 4탭, 모임 1열 |
| `431~599px` | 앱 최대 `520px`, 본문 좌우 `24px` | 하단 4탭, 가로 스크롤 필터 |
| `600~1023px` | 앱 최대 `960px`, 유동 여백 | 하단 탭, 모임 2열, 통계·사진첩 3열 |
| `≥1024px` | 앱 최대 `1440px`, 본문 왼쪽 `112px` 제외 | 왼쪽 세로 탭 레일 `112px`, 모임 3열 |

하단 탭은 반투명 남색 표면, 배경 블러, 가는 상단 경계선을 쓴다. 선택 탭은 주황 글자·채움 아이콘·연한 주황 캡슐로 구분한다. PC에서는 같은 네 탭을 세로 레일로 옮긴다.

`--nav-h`는 `calc(62px + env(safe-area-inset-bottom))`다. 고정 버튼·저장 바·채팅 입력창·본문 하단 여백이 탐색 영역과 겹치지 않게 한다. 모임 만들기 플로팅 버튼이 있는 매칭 화면은 목록 끝 여백을 추가한다.

## 5. 공용 컴포넌트

| 컴포넌트 | 클래스·구조 | 사용 기준 |
| --- | --- | --- |
| 카드 | `.card` | 관련 정보를 하나의 표면으로 묶음 |
| 주요 행동 | `.cta` | 전폭, 높이 `52px`, 주황 그라데이션 |
| 보조 행동 | `.cta.line` | 중립색 테두리, 취소·일반 참가 |
| 약한 강조 행동 | `.cta.soft` | 옅은 주황 배경, 참가 중 상태 등 |
| 작은 행동 | `.cta.sm` | 높이 `44px` |
| 선택 칩 | `.chip`, `.chip.on` | 선택 시 주황 테두리·배경·작은 점 |
| 직접 추가 | `.chip.add` | 점선 테두리, 기존 선택지와 구분 |
| 정보 태그 | `.tag`, `.tag.or` | 관심사·지역 등 읽기용 정보 |
| 상태 배지 | `.badge`, `.or`, `.ok`, `.moon` | 참가·완료·시스템 상태 |
| 아바타 묶음 | `.avs` | 겹친 원형 아바타와 초과 인원 |
| 비율·진행 | `.bar > .fill` | 주황 채움과 설명·수치 병기 |
| 원형 아이콘 버튼 | `.ib` | 상단 행동·닫기, 접근 가능한 이름 제공 |
| 상단·섹션 | `.top`, `.sec` | 화면 제목과 행동, 섹션 제목과 보조 수치 |
| 바텀 시트 | `.sheetwrap` 안의 `.dimbg` + `.sheet` | 배경을 어둡게 하고 현재 맥락 위에서 상세·설정 처리 |
| 시트 내부 | `.grip` + `.sh` + `.sbody` | 손잡이, 제목·닫기, 스크롤 본문 |
| 로딩 | `.sk`, `.skcard` | 실제 콘텐츠와 유사한 형태의 스켈레톤 |
| 빈 상태 | `.empty` 안의 아이콘 + `<b>` + 설명 + `.cta` | 현재 비어 있는 이유와 다음 행동 |
| 알림 | `#toast` | 화면 상단의 짧은 처리 결과 안내 |

비활성 CTA는 `--card2` 표면·`--tx3` 글자·그림자 제거로 표현하고 실제 `disabled` 상태를 함께 적용한다. 입력 오류는 해당 입력 근처의 `.eerr`에 남겨 수정할 수 있게 한다. 탈퇴·취소 등 확인이 필요한 행동은 `askConfirm(title, desc, okLabel, danger)`로 같은 디자인의 확인 시트를 사용한다.

## 6. 화면별 구성

### 홈 — 내 우주

상단 브랜드·설명, 다음 할 일·사용 가이드 진입, 궤도 시각화, 연결 통계, 계열사 커넥션 목록으로 구성한다. 우주가 화면의 시각적 중심을 맡고 목록이 구체적인 연결 상태를 설명한다.

아는 사람이 생긴 계열사와 내 계열사만 궤도에 배치한다. 나머지 계열사는 아래 목록에서 미개척 상태로 확인한다. 행성은 상세로 들어가는 조작 대상이며, 겹치지 않는 터치 영역을 유지한다. 내 계열사 상세에서는 동료 연결을 위성으로 표현한다.

### 매칭 — 모임 둘러보기

내 설정 요약 → 가로 필터 → 모임 카드 → 모임 만들기 플로팅 버튼 순서다. 모임 카드는 다음 위계를 유지한다.

1. 단색 표면의 이모지 타일과 모임 제목
2. 위치·시간대·인원 메타와 관심사 태그
3. 가는 구분선과 멤버 아바타, 아는 얼굴·새로운 사람 안내
4. 아는 얼굴이 있을 때 주황 비율 막대와 백분율
5. 전폭 참가 버튼

일반 상태는 `.cta.line`의 `참가하기`, 참가 후에는 `.cta.soft`의 `참가 중 · 채팅방 열기`, 정원이 차면 비활성 마감 버튼으로 표시한다. AI 추천 이유는 제목을 눌러 여는 상세 시트에 두고 목록에 이유 패널·취향 일치 배지를 추가하지 않는다.

### 채팅 — 대화에서 만남으로

목록은 큰 모임 이모지 타일, 제목·인원, 최근 메시지, 시간, 읽지 않음 배지로 구성한다. 채팅방은 상단 정보·약속 배너, 메시지 영역, 하단 입력창으로 나눈다. 모바일에서는 채팅 중에도 하단 탭으로 이동할 수 있다.

내 말풍선은 주황 그라데이션, 상대 말풍선은 어두운 카드 표면, 시스템 안내는 달빛색 캡슐로 구분한다. 연속 메시지는 아바타·이름을 반복하지 않고 묶음 끝에 시간을 둔다. 처음 대화할 때는 입력창 위의 첫 인사 칩으로 시작을 돕는다.

익명 닉네임을 기본으로 표시한다. 나와 상대가 모두 만남 완료한 관계만 실명 공개 대상이므로, 같은 방에서도 익명과 실명이 함께 보일 수 있다. 베일 벗기기 연출 뒤에는 사진첩과 연결 상태로 결과를 확인하게 한다.

### 약속 잡기 — 시간과 장소

현재 신규 약속 흐름은 `날짜 범위 설정 → 가능한 시간·장소 저장 → 방장 최종 확정`이다. 채팅방 `＋` 메뉴의 `약속 잡기 · 시간과 장소`에서 진입한다.

- 시간표는 가로 날짜, 세로 시간의 30분 단위 표다. 날짜 헤더와 시간축을 고정하고 표 안에서 스크롤한다.
- 선택한 셀은 주황 배경·안쪽 테두리로, 전원 가능한 셀은 초록 글자·테두리로 구분한다. 가능 인원도 함께 표시한다.
- 장소 후보는 번호, 이름, 분류, 주소·설명, 상세 링크로 구성한다. 지도와 후보 목록의 선택을 동기화한다.
- 저장과 확정을 구분한다. 참여자는 자기 가능 시간·장소를 저장하고, 방장은 응답 비교 후 최종 시간·장소를 확정한다.
- 검색 실패는 안내와 다시 시도 행동으로 표현한다. 선택 가능한 실제 장소처럼 보이는 가짜 결과를 만들지 않는다.

기존 데이터의 전원 투표·시간 경과 확정 UI는 호환 경로다. 새 화면의 기본 흐름은 통합 시간표와 방장 확정을 따른다.

### 프로필 — 바꿀 수 있는 개인 설정

아바타·닉네임·계열사 히어로 카드와 `기본 정보`, `나에 대해`, `매칭 성향` 섹션을 사용한다. 입력, 복수 선택 칩, 세그먼트 버튼, 스위치로 설정의 성격을 구분한다.

첫 설정에는 4항목 체크리스트, 완료 수, 진행 막대를 제공한다. 각 항목을 누르면 해당 설정으로 이동한다. 사용 가이드는 접어서 보관하고 다시 펼칠 수 있게 한다. 선호 지역의 필수 여부와 추천 범위에 미치는 영향을 입력 옆에서 설명한다.

수정 시에만 `저장 · 변경 사항 있음` 바를 표시한다. `이 설정으로 모임 둘러보기`는 변경 사항을 저장한 뒤 매칭으로 이동한다. 저장 실패 시에는 입력 내용과 현재 화면을 유지한다.

## 7. 아이콘·문구·모션

### 아이콘과 문구

UI 아이콘은 [config.js](src/js/config.js)의 `ico(name)` 인라인 SVG를 재사용한다. `pin`, `clock`, `users`, `chev`, `check`, `x`, `plus`, `spark`, `moon`, `cal` 등이 있으며 `currentColor`로 주변 글자색을 따른다. 모임 이모지·아바타는 콘텐츠로 사용할 수 있다.

UI 문구는 한국어로 짧고 친근하게 쓴다. 버튼은 `참가하기`, `가능 시간과 장소 저장`, `모임 둘러보기`처럼 결과를 예측할 수 있는 행동으로 적는다. 오류·빈 상태는 이유와 해결 행동을 함께 설명한다. 화면에 구현 용어 대신 사용자가 결정하는 데 필요한 정보를 보여 준다.

### 움직임

| 용도 | 현행 동작 |
| --- | --- |
| 눌림 | 약 `0.12s`, `scale(.97)` |
| 탭 진입 | `0.22s`, 투명도와 아래 `6px`에서 올라오는 움직임 |
| 상단 토스트 | `0.38s`, 위쪽에서 슬라이드 |
| 비율 막대 | 약 `0.45s`, 채움 너비 변화 |
| 행성 | 궤도별 속도가 다른 회전, 새로운 연결의 합류 연출 |

일반 전환에는 `--ease: cubic-bezier(.2,.8,.2,1)`, 탄성 있는 전환에는 `--spring: cubic-bezier(.34,1.4,.4,1)`을 사용한다. 반복 우주 애니메이션은 콘텐츠의 읽기와 조작을 방해하지 않을 정도로 유지한다.

`prefers-reduced-motion: reduce`에서는 전환을 제거하거나 짧게 하고, 궤도 회전은 멈추며 행성 합류는 최종 상태로 즉시 표시한다. 애니메이션이 없어도 관계와 완료 상태를 이해할 수 있어야 한다.

## 8. 변경 시 확인 기준

- 375×812에서 제목·카드·입력이 넘치지 않고 마지막 콘텐츠까지 접근할 수 있는가?
- 태블릿 2열·PC 3열과 왼쪽 탭 레일에서 내용의 읽기 순서가 유지되는가?
- 입력 글자는 16px 이상이며 노치·홈 인디케이터·키보드에 핵심 행동이 가려지지 않는가?
- 조작 대상은 최소 44×44px이며 터치·키보드로 선택·닫기·취소할 수 있는가?
- 선택·성공·오류를 색 외의 문구·아이콘·테두리로도 구분할 수 있는가?
- 포커스가 보이고, 아이콘 버튼에는 접근 가능한 이름이 있는가?
- 로딩·빈 상태·실패·비활성·저장 전후 상태에 필요한 안내가 있는가?
- 모션 감소 설정에서도 행성·시간표·만남 완료 결과를 확인할 수 있는가?
- 새 스타일이 기존 공용 클래스와 `:root` 토큰을 재사용하는가?
- 매칭 참가 → 채팅 → 시간·장소 조율 → 확정 → 상호 만남 완료 → 홈 연결 표시가 이어지는가?

현재 CSS의 일부 칩·원형 버튼·스위치는 기본 시각 크기가 44px보다 작다. 기존 선언이 모두 접근성 기준을 충족한다고 가정하지 말고 실제 클릭 영역을 확인하며, 해당 컴포넌트를 수정할 때 최소 터치 영역을 확보한다. 작은 보조 글자의 대비도 실제 배경과 함께 확인한다.

시각 확인은 `src/index.html?demo=1`로 로컬 데모를 열어 진행할 수 있다. UI 코드 변경 시에는 Node.js 24 이상에서 `npm test`로 관련 계약과 회귀 검사를 함께 확인한다.
